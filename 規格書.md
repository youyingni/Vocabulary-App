# Vocabulary App 更新規格書

本次更新已依照您的需求，將重心轉移至「多益單字學習」與「重複測驗熟練度追蹤」，並移除了不需要的 OCR 相關功能。

## 1. 移除 OCR 相關功能
由於您主要在電腦版上使用，且不需要手寫 OCR 的功能，系統已清理並移除了以下相關元件，讓 App 運作更輕量快速：
- **介面移除**：側邊欄的「📷 筆記 OCR 匯入」按鈕已移除。
- **彈出視窗**：原本的 OCR 匯入、設定 API 網址、Gemini 糾錯介面已全數刪除。
- **後端整合**：移除了 Python 伺服器 (PaddleOCR / EasyOCR) 的相關呼叫，前端 `app.js` 與 `styles.css` 裡的相關程式碼也已清理乾淨。

## 2. 內建多益 (TOEIC) 單字與分類
根據您提供的 PDF 資料，已將單字整理成四個專屬資料夾，並預設載入於您的單字庫中。只需**重新整理網頁**即可自動載入新單字。

- 📁 **TOEIC 常用片語與句型** (藍色資料夾)
  包含 `get off`, `take off`, `on account of`, `beat around the bush` 等。
- 📁 **TOEIC 商業、辦公與職涯** (黃色資料夾)
  包含 `reputation`, `discount`, `shareholder`, `merger`, `agenda` 等。
- 📁 **TOEIC 日常、生活與雜項** (綠色資料夾)
  包含 `caterer`, `appliance`, `thunderstorm`, `pharmaceutical` 以及大學四個年級的英文等。
- 📁 **TOEIC 易混淆與相近字群** (紫色資料夾)
  包含 `mimic / comic / panic`, `penalty / penal`, `drawback / draw back` 等。

## 3. 多次測驗熟練度系統 (Mastery System)
為了確認您「真的會了」，而不是瞎猜或只看一次就以為懂了，我們在卡片測驗模式加入了熟練度系統：

- **✅ 記得 / ❌ 忘記**：測驗模式原本底下的「加星號」與「下一張」按鈕，已改為「✅ 記得」(也可透過右滑觸發) 以及「❌ 忘記」(也可透過左滑觸發)。
- **熟練度計算**：
  - 點擊「✅ 記得」：該單字的熟練度 + 1。
  - 點擊「❌ 忘記」：該單字的熟練度將**立刻歸零重置**，這代表您必須連續答對多次才算熟練，不能中斷。
- **🔥 已熟練徽章**：當某個單字連續答對 **3 次**，在單字列表的卡片上會自動顯示「🔥」火焰徽章，並且卡片背景會變更為綠色，代表您已完全掌握此單字！
